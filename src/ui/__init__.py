"""
UI layer (Streamlit)
====================
Helper package for the Streamlit dashboard. The actual pages live in the
top-level ``app.py`` + ``pages/`` (Streamlit's multipage convention); this
package holds the cached service factory and shared render components so the
pages stay thin.

Run the app with:  streamlit run app.py
"""
