# Email Intelligence Platform
# Python 3.9 - 3.12 recommended (some AI wheels lag the newest Python).
#
# Install everything:   pip install -r requirements.txt
# The existing ML stack and the new AI stack are separated so you can install
# just the core if you only want the original sentiment pipeline.

# ─────────────────────────────────────────────────────────────────────────────
# Core ML  (ORIGINAL pipeline — required for the existing features)
# ─────────────────────────────────────────────────────────────────────────────
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# ── NLP ───────────────────────────────────────────────────────────────────────
nltk>=3.8.1

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ─────────────────────────────────────────────────────────────────────────────
# AI Intelligence layer  (NEW — optional but enables LLM/RAG/Chat features)
# Everything below degrades gracefully if not installed.
# ─────────────────────────────────────────────────────────────────────────────

# RAG: embeddings + vector store
sentence-transformers>=2.2.2
chromadb>=0.4.22

# Local LLM client (talks to a locally running Ollama daemon — no cloud APIs)
ollama>=0.1.7

# Dashboard
streamlit>=1.30.0

# Config (optional — config.py has a stdlib fallback .env parser, so this is
# only needed if you want full python-dotenv behaviour)
python-dotenv>=1.0.0

# ── Standard library (no install needed) ──────────────────────────────────────
# sqlite3, imaplib, email, re  — part of the Python standard library

# ─────────────────────────────────────────────────────────────────────────────
# Dev / Test
# ─────────────────────────────────────────────────────────────────────────────
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.0.0
